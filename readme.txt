g00351263
Welcome to the FinalDataRep2018 wiki!

Following are Important for Running the App
go into project folder by typing cd and project name and install all dependencies if not compiling after clone.

following are the installs for them ?? this following are important to install before running the app

npm install --save @angular/material @angular/cdk @angular/animations

npm install ngx-spinner --save

npm install express -- save

npm install mongoose

if still showing error type audit to check what more needed and install those dependencies. audit fix to fix them all

ng serve to be typed in root folder for applications to start.

node .\backend\server should be typed in to start server for communication with database of blog.

open google and type localhost:4200 to run the app.

open google and type in localhost:8081 to check server working